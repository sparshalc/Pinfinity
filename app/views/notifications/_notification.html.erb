<div>
  <div class="d-flex">
    <div class="user_details">
      <% if user&.email.present? && pin&.name.present? %>
        <%= user.email %> commented on <%= link_to(pin.name, board_pin_path(pin.board_id, pin), data: { turbo_frame: '_top' }) %>
      <% else %>
        <span class="text-danger">The comment has been deleted by the user</span>
      <% end %>
      <% if unread %>
        <span class="badge bg-danger">Unread</span>
      <% else %>
        <span class="badge bg-success">Read</span>
      <% end %>
    </div>
  </div>
  <hr>
</div>
