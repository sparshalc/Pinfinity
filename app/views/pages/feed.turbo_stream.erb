<%= turbo_stream.append :pin do %>
  <%= render @pin %>
<% end %>
<%= turbo_stream.replace :pagination do %>
  <% if @pagy.next.present? %>
    <%= turbo_frame_tag :pagination,
                loading: :lazy,
        src: feed_path(format: :turbo_stream, page: @pagy.next) %>
  <% end %>
<% end %>
